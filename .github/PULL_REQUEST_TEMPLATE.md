## What changed and why

<!-- The problem this solves or the feature this adds. Link an issue if there is one. -->

## How to verify

<!-- What you actually did to check this works: commands run, screenshots, a URL you tested against. -->

## Checklist

- [ ] `npm test` passes locally (`cd backend && npm test`)
- [ ] Tested the affected flow in a browser, not just read the diff
- [ ] No secrets (API keys, `.env` contents) in this diff
